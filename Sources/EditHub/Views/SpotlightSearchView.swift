import SwiftUI

// The ⌘K Spotlight switcher was replaced by the rail's sliding search field
// (see RootSplitView). This file is intentionally left as an empty placeholder
// so the Xcode project's file reference stays valid; remove the reference from
// the project to delete it entirely.
